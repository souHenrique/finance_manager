version: '3.8'
services:
  postgres:
    image: postgres:16-alpine
    container_name: finance_manager_db
    env_file:
      - .env
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always
volumes:
  postgres_data: